"""
Legacy setup.py — kept for pip install git+https://github.com/... compatibility.
The canonical config lives in pyproject.toml.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
